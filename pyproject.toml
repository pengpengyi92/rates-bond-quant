[project]
name = "ficc-rates-bond-quant-demo"
version = "0.0.1"
description = "Educational FICC rates bond quant demo for pricing, duration, convexity, scenarios, and PnL."
requires-python = ">=3.10"
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
